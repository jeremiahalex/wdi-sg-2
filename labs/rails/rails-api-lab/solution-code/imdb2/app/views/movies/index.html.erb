<h2 class='text-center'>Our Current Movies</h2>
<hr>
<br>
<div class="row text-center">
  <% @movies.each do |movie| %>
    <div class="col-md-4">
      <%= link_to(image_tag(movie.thumbnail), movie_path(movie)) %>
      <p><%= movie.title %></p>
    </div>
<% end %>
</div>
